# deploy

Generate CI/CD configuration files for publishing your presentation.

```sh
slidesk deploy -t github mytalk
slidesk deploy -t gitlab mytalk
slidesk deploy -t link mytalk
```

## Options

```
  -t, --target <param>      'github', 'gitlab' or 'link'
```
