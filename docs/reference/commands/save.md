# save

Export your presentation as a standalone static HTML site.

```sh
slidesk save -t public mytalk
```

## Options

```
  -t, --target <path>        output directory (default: "public")
  -c, --conf <file>          custom slidesk.toml path
  -l, --lang <code>          language version
```
