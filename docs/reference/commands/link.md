# link

Interact with the slidesk.link hub.

```sh
slidesk link login         # authenticate
slidesk link host -n …    # host a presentation (72h, max 100 MB)
slidesk link push          # publish talk metadata via link.yml
```
