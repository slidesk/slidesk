# Includes

Split long presentations into multiple files with `!include()`:

```markdown
!include(path/of/sdf or md file)
```

You can include a directory — files are included alphabetically:

```markdown
!include(path/of/directory)
```
